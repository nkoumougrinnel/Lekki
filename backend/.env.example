SECRET_KEY=change-this-in-production
GEMINI_API_KEY=
DEBUG=true

# Clé pour POST /internal/embed (optionnel)
# INTERNAL_API_KEY=lekki-internal-secret-key

# Base (optionnel — défaut : ./data/wiki.db)
# DATABASE_URL=sqlite+aiosqlite:///./data/wiki.db
# SYNC_DATABASE_URL=sqlite:///./data/wiki.db

# Mot de passe des comptes créés par `python -m scripts.seed`
SEED_PASSWORD=lekki123