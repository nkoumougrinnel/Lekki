# Lekki Backend — dependances principales

# API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Base de donnees
sqlalchemy>=2.0.0
aiosqlite>=0.20.0
alembic>=1.13.0

# Configuration et validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Authentification
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# LLM distant — Gemini (SDK officiel)
google-genai>=1.0.0

# LLM distant — Groq et Cerebras (API OpenAI-compatible via HTTP)
httpx>=0.27.0

# RAG (chunking, embeddings)
langchain-text-splitters>=0.2.0
numpy>=1.26.0

# Scripts et tests d integration
requests>=2.31.0

# Tests
pytest>=8.0.0
pytest-asyncio>=0.24.0
